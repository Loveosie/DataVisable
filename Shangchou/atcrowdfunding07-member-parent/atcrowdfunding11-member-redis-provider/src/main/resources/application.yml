server:
  port: 3000
spring:
  application:
    name: atguigu-crowd-redis
  # 配置Redis IP地址
  redis:
    host: 127.0.0.1
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka